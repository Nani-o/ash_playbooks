---
- hosts: all
  tasks:
    - debug:
        msg: "{{ item }}"
      loop:
        - "{{ name_survey }}"
        - "{{ choice }}"
        - "{{ multiple_choice }}"
        - "{{ example }}"

    - name: "Sleep for 60 seconds"
      ansible.builtin.command: sleep 60
...